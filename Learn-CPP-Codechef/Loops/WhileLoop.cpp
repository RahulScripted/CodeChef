// Write a program which does the following:

// 1.   Declare a int variable a and initialise it to 0.

// 2.   Use the syntax above to create a loop, output the following to the console:

// 3.   Print a in separate lines as long as it is less than 7.

// 4.   Increment a by 1 in each iteration.





#include <bits/stdc++.h>
using namespace std;

int main() {
    int a = 0;
    while (a < 7 ) {
        cout << a << endl;
        a = a + 1;
    }
}