// In the last lesson, we wanted space between numbers so added an empty space inside quotes " ". If we want any other text in between the outputs we can provide that text inside " " sign.



#include <bits/stdc++.h>
using namespace std;

int main() {
    cout << 7 << " and " << 3;
}