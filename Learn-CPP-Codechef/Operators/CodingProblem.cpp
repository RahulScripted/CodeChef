// You are given the age of a person as the variable age.Will this person be able to vote? Output 1 if yes, else 0, assuming that the voting age is 18 i.e., a person's age has to be greater than or equal to 18 to vote.




#include <bits/stdc++.h>
using namespace std;

int main() {
    int age = 20;
    cout<<(age >= 18)<<endl;
}
