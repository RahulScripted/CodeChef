// Write a program which does the following

// 1.  Take input for two integer variables a & b

// 2.  Output "Coding is Fun!" to the console if a is greater than b.




#include <bits/stdc++.h>
using namespace std;

int main() {
    int a;
    int b;
    cin >> a;
    cin >> b;
    if ( a > b ) cout << "Coding is Fun!";
}