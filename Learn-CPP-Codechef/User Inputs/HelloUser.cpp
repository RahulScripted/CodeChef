// Declare a string variable x. Accept a text user input - the name of the user - and store it in the variable x. Print to the console "Hello" before the user defined name.



#include <bits/stdc++.h>
using namespace std;

int main() {
    string x;
    cin >> x;
    cout << "Hello " << x;
}