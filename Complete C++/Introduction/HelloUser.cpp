// Write a program which does the following: Declare a string variable x. Accept a text user input - the name of the user - and store it in the variable x. Output and print to the console "Hello" before the user defined name.





#include <iostream>
using namespace std;

int main() {
    string s; 
    cin >> s;
    cout << "Hello " << s;
}